# circleci manifest yml
version: 2
jobs:
  build:
    docker:
            - image: nat-streaming-server:latest
    steps:
      - checkout
      - run:
          name: nats-streaming
          command: nat-streaming-server -m 8222
